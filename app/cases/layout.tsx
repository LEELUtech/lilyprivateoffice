import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Engagements & Operational Outcomes — Lily Chystofat",
  description: "A curated record of high-stakes interventions across capital markets, succession dynamics, and organizational intelligence.",
};

export default function CasesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
